import EditListingClient from "../[listingId]/EditListingClient";

/**
 * STATIC EDIT ANCHOR for Marketplace (Capacitor Compatibility)
 */
export const dynamic = "force-static";

export default function EditListingSafePage() {
  return <EditListingClient />;
}
