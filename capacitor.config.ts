import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.powerapp.template',
  appName: 'Power Platform',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
